// import VscodeKeyboardEvent, { KeyEvent } from '../vscode-keyboard-event';

// const createInstance = (e: KeyEvent) => new VscodeKeyboardEvent(e);

describe('VscodeKeyboardEventクラスのテスト', () => {
  it('デフォルトで設定されたタブのサイズが 4 であることをテストする', () => {
    // const instance = createInstance();
    // expect(instance.TAB_SIZE).toBe(4);
  });
  describe('イベントテスト', () => {
    it('エンター押下', () => {
      // createInstance();
    });
    it('コマンド+エンター押下', () => {
      // createInstance();
    });
    it('タブ押下', () => {
      // createInstance();
    });
    it('シフト+タブ押下', () => {
      // createInstance();
    });
  });
});
